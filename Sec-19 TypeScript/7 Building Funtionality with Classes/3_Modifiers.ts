/* 
class Vehicle {
  public honk(): void {
    console.log("beep");
  }
}

class Car extends Vehicle {
  private drive(): void {
    console.log(`vroom`);
  }
  startDrivingProcess(): void {
    this.drive();
  }
}

********* with private member we can't access member outside the class so we define method for them  ***********

const car = new Car();
car.startDrivingProcess();

*/
